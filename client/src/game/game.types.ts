import { RoomState } from "../../../server/src/room/room.state";

export { RoomState };

export interface Position {
  x: number;
  y: number;
}

export interface Velocity {
  x: number;
  y: number;
}
